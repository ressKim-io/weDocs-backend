<?xml version="1.0" encoding="UTF-8"?>
<!--
  logback 설정 — Spring Boot 기본 콘솔 유지 + 에러 전용 JSON 파일 추가.
  에러 전용 파일은 AI 분석 파이프라인이 읽는 구조화된 입력이 된다.
  prod에서는 로그 수집기(fluentbit/vector)가 이 파일을 tail한다.
-->
<configuration>
    <!-- Spring Boot 기본 콘솔 출력 (컬러, 패턴) 재사용 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- 에러 전용 JSON 파일 appender — ERROR 이상만 -->
    <appender name="ERROR_JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/error.json</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/error.%d{yyyy-MM-dd}.%i.json</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.JsonEncoder"/>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ERROR_JSON_FILE"/>
    </root>
</configuration>
